<Window x:Class="ExcelProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExcelProject"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920" WindowState="Maximized">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="0.65*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                <ComboBox x:Name="fontfamily"/>
                <ComboBox x:Name="fontsize"/>
                <Button x:Name="size_plus"/>
                <Button x:Name="size_minus"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="fontweight"/>
                <Button x:Name="fontstyle"/>
                <Button x:Name="fontdecoration"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Border Margin="20 0 10 0" BorderBrush="Gray" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style TargetType="Button" x:Key="FN_BTN">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="FontSize" Value="24"/>
                            <Setter Property="FontStyle" Value="Italic"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Width" Value="60"/>
                            <Setter Property="Height" Value="60"/>
                        </Style>
                    </StackPanel.Resources>
                    <Button Content="X">
                        <Button.Style>
                            <Style TargetType="Button"  BasedOn="{StaticResource FN_BTN}">
                                <Style.Triggers>
                                    <Trigger Property="Button.IsMouseOver" Value="True">
                                        <Setter Property="Button.Foreground" Value="Red"/>
                                        <Setter Property="Button.Background" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="fx">
                        <Button.Style>
                            <Style TargetType="Button"  BasedOn="{StaticResource FN_BTN}">
                                <Style.Triggers>
                                    <Trigger Property="Button.IsMouseOver" Value="True">
                                        <Setter Property="Button.Foreground" Value="Blue"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
            <Border BorderBrush="Gray" BorderThickness="1">
                <TextBox VerticalContentAlignment="Center" VerticalAlignment="Stretch" Width="1760" FontSize="24" >
                    
                </TextBox>
            </Border>
            <!--függvényillesztő-->
        </StackPanel>
        <Grid Grid.Row="2">
            <!--exceltable-->
        </Grid>
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            
        </StackPanel>
        
    </Grid>
</Window>
